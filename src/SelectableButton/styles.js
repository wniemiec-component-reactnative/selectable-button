import { StyleSheet } from 'react-native'

export default StyleSheet.create({

  btnTitle: {
    fontSize: 15
  },
  btnArea: {
    justifyContent: 'center',
    alignItems: 'center',
    paddingLeft: 10,
    paddingRight: 10,
    borderRadius: 10,
    minWidth: 40,
    height: 40,
    margin: 5
  }
});
